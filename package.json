{
  "name": "campuspulse-ai",
  "version": "1.0.0",
  "description": "CampusPulse AI — Intelligent campus infrastructure incident detection system",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npx -y concurrently -n \"backend,frontend\" -c \"blue.bold,cyan.bold\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "test": "npm --prefix backend run test",
    "db:init": "npm --prefix backend run db:init",
    "db:seed": "npm --prefix backend run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "campuspulse",
    "incident-intelligence",
    "ai",
    "hackathon",
    "sqlite",
    "react",
    "express"
  ],
  "author": "CampusPulse AI Team",
  "license": "ISC"
}
